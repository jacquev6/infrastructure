# THIS FILE IS GENERATED by Terraform. MANUAL CHANGES WILL BE LOST.

all:
  vars:
    ansible_user: ansible

  hosts:

  children:
    # Special case, not yet in any other group
    web_server:
      hosts:
        15.188.248.48:
      vars:
        ansible_user: ubuntu

    # By OS
    ubuntu:
      hosts:
      vars:
        bootstrap_user: ubuntu
        bootstrap_password: ubuntu

    # By status
    bootstrapped:
      hosts:
