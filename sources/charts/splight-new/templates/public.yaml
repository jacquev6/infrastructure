apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.baseName }}-public
spec:
  selector:
    matchLabels:
      app: {{ .Values.baseName }}-public
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ .Values.baseName }}-public
    spec:
      containers:
        - name: {{ .Values.baseName }}-public
          image: jacquev6/splight:{{ .Values.version }}.public-frontend.public
          ports:
            - containerPort: 80
          env:
            - name: SPLIGHT_API_URL
              value: "{{ .Values.apiPublicUrl }}"
          resources:
            requests:
              cpu: 10m
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.baseName }}-public
spec:
  type: NodePort
  selector:
    app: {{ .Values.baseName }}-public
  ports:
    - port: 80
